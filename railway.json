{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": ["Graduation-Ideas/client/**"]
  },
  "deploy": {
    "startCommand": "npm --prefix Graduation-Ideas/client run preview -- --host 0.0.0.0 --port $PORT",
    "numReplicas": 1
  }
}
